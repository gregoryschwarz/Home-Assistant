---
phase: 04-voice-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_voice_pipeline.py
autonomous: false
requirements:
  - VOICE-01
  - VOICE-02
user_setup:
  - service: wyoming-whisper
    why: "Local STT for voice pipeline"
    env_vars: []
    dashboard_config:
      - task: "Install Wyoming Whisper add-on"
        location: "HA Settings > Add-ons > Add-on Store > search 'Whisper' > Install > Start"
      - task: "Add Wyoming Protocol integration for Whisper (if not auto-discovered)"
        location: "HA Settings > Devices & Services > Add Integration > Wyoming Protocol > host=localhost, port=10300"
  - service: wyoming-piper
    why: "Local TTS for voice pipeline"
    env_vars: []
    dashboard_config:
      - task: "Install Wyoming Piper add-on"
        location: "HA Settings > Add-ons > Add-on Store > search 'Piper' > Install > Start"
      - task: "Add Wyoming Protocol integration for Piper (if not auto-discovered)"
        location: "HA Settings > Devices & Services > Add Integration > Wyoming Protocol > host=localhost, port=10200"
  - service: openwakeword
    why: "Wake word detection (optional)"
    env_vars: []
    dashboard_config:
      - task: "Install openWakeWord add-on (optional)"
        location: "HA Settings > Add-ons > Add-on Store > search 'openWakeWord' > Install > Start"
  - service: ha-pipeline
    why: "Voice assistant pipeline configuration"
    env_vars: []
    dashboard_config:
      - task: "Create Assist pipeline selecting ha_ai_agent as conversation backend"
        location: "HA Settings > Voice Assistants > Add Assistant"

must_haves:
  truths:
    - "assist_pipeline can discover HaAiConversationAgent by entity_id conversation.ha_ai_agent"
    - "Agent entity_id has the conversation. prefix required for pipeline routing"
    - "_async_handle_message sets speech on IntentResponse so TTS can read it aloud"
    - "ConversationResult contains non-empty speech text for the pipeline to forward to TTS"
  artifacts:
    - path: "tests/test_voice_pipeline.py"
      provides: "Unit tests proving agent is pipeline-discoverable and produces TTS-compatible output"
      min_lines: 60
      contains: "test_agent_discoverable_by_pipeline"
  key_links:
    - from: "custom_components/ha_ai_agent/conversation.py"
      to: "homeassistant.components.conversation.async_get_agent_info"
      via: "ConversationEntity registration makes entity discoverable"
      pattern: "async_get_agent_info.*conversation\\.ha_ai_agent"
    - from: "custom_components/ha_ai_agent/conversation.py"
      to: "homeassistant.components.assist_pipeline"
      via: "IntentResponse.async_set_speech populates speech for TTS stage"
      pattern: "async_set_speech"
---

<objective>
Validate that the existing HaAiConversationAgent is fully pipeline-compatible by writing unit tests (VOICE-01, VOICE-02), then guide the user through HA UI pipeline configuration and end-to-end voice verification.

Purpose: Phase 4 requires zero new Python code in the component. The work is: (1) prove via automated tests that the agent is discoverable by assist_pipeline and produces TTS-compatible output, (2) configure the voice pipeline in HA UI, (3) verify voice end-to-end.

Output: `tests/test_voice_pipeline.py` with 4 passing tests, configured HA voice pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voice-pipeline/04-CONTEXT.md
@.planning/phases/04-voice-pipeline/04-RESEARCH.md
@.planning/phases/04-voice-pipeline/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From custom_components/ha_ai_agent/conversation.py:
```python
class HaAiConversationAgent(ConversationEntity):
    _attr_has_entity_name = True
    _attr_name = "HA AI Agent"
    # -> entity_id = "conversation.ha_ai_agent"

    @property
    def supported_languages(self) -> list[str]:
        return ["fr", "en"]

    async def _async_handle_message(
        self, user_input: ConversationInput, chat_log: ChatLog,
    ) -> ConversationResult:
        # ... routes via IntentRouter / ClaudeClient ...
        intent_response = conversation_intent.IntentResponse(language=user_input.language)
        intent_response.async_set_speech(response_text)
        return ConversationResult(response=intent_response, conversation_id=user_input.conversation_id)
```

From tests/conftest.py:
```python
@pytest.fixture
async def mock_config_entry(hass: HomeAssistant, enable_custom_integrations) -> MockConfigEntry:
    await async_setup_component(hass, "homeassistant", {})
    entry = MockConfigEntry(
        domain=DOMAIN,
        data={CONF_API_KEY: "sk-ant-test-key-for-testing"},
        entry_id="test_entry_id",
        title="HA AI Agent",
        version=1,
        unique_id=DOMAIN,
    )
    entry.add_to_hass(hass)
    return entry
```

From HA 2026.3.4 source (verified in 04-RESEARCH.md):
```python
# homeassistant.components.conversation.async_get_agent_info
def async_get_agent_info(hass, agent_id) -> AgentInfo | None:
    # Returns AgentInfo with .id attribute if agent found

# AgentInfo has at minimum: .id (str)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create voice pipeline unit tests</name>
  <files>tests/test_voice_pipeline.py</files>
  <read_first>
    - tests/conftest.py (fixtures: mock_config_entry, hass_config_dir, enable_custom_integrations)
    - tests/test_conversation.py (pattern: how existing tests set up and query the agent entity)
    - custom_components/ha_ai_agent/conversation.py (verify _async_handle_message sets speech via async_set_speech)
    - custom_components/ha_ai_agent/const.py (DOMAIN constant)
  </read_first>
  <behavior>
    - test_agent_discoverable_by_pipeline: after setup, `async_get_agent_info(hass, "conversation.ha_ai_agent")` returns non-None AgentInfo with `.id == "conversation.ha_ai_agent"` (VOICE-01)
    - test_agent_entity_id_format: after setup, entity states filtered by DOMAIN have entity_id starting with `"conversation."` — prerequisite for pipeline dot-notation routing (VOICE-01)
    - test_response_speech_set: calling `_async_handle_message` with a known command returns a ConversationResult where `result.response.speech["plain"]["speech"]` is a non-empty string (VOICE-02)
    - test_conversation_result_has_speech: calling `_async_handle_message` with a known command returns a ConversationResult where `result.response` is an IntentResponse instance and `result.response.speech` dict is non-empty (VOICE-02)
  </behavior>
  <action>
    Create `tests/test_voice_pipeline.py` with 4 test functions. Follow the exact patterns from `tests/test_conversation.py`:

    1. **Imports:** Use `from homeassistant.components.conversation import async_get_agent_info, ConversationInput, ChatLog, ConversationResult, intent as conversation_intent` and `from custom_components.ha_ai_agent.const import DOMAIN`. Import `unittest.mock.AsyncMock, MagicMock, patch`.

    2. **test_agent_discoverable_by_pipeline(hass, mock_config_entry):**
       - `await hass.config_entries.async_setup(mock_config_entry.entry_id)`
       - `await hass.async_block_till_done()`
       - `agent_info = async_get_agent_info(hass, "conversation.ha_ai_agent")`
       - `assert agent_info is not None`
       - `assert agent_info.id == "conversation.ha_ai_agent"`
       - Per D-07, VOICE-01: This proves assist_pipeline can find our agent.

    3. **test_agent_entity_id_format(hass, mock_config_entry):**
       - Setup same as above.
       - `states = hass.states.async_all("conversation")`
       - `agent_states = [s for s in states if DOMAIN in s.entity_id]`
       - `assert len(agent_states) == 1`
       - `assert agent_states[0].entity_id.startswith("conversation.")`
       - Per VOICE-01: Pipeline requires dot-notation entity_id for routing.

    4. **test_response_speech_set(hass, mock_config_entry):**
       - Setup the entry.
       - Patch `custom_components.ha_ai_agent.intent_router.IntentRouter.async_route` to return `"Salon allume."` (simulate local rule match).
       - Create `ConversationInput(text="allume le salon", context=Context(), conversation_id=None, device_id=None, language="fr", agent_id=None, satellite_id=None)`. HA 2026.x ConversationInput requires both `device_id` and `satellite_id` as positional args — pass both as None.
       - Get the entity from `hass.data["entity_components"]["conversation"].get_entity("conversation.ha_ai_agent")`.
       - Create a mock `ChatLog` using `MagicMock(spec=ChatLog)` with `async_add_assistant_content_without_tools = MagicMock()`.
       - Call `result = await entity._async_handle_message(user_input, chat_log)`.
       - `assert result.response.speech["plain"]["speech"] == "Salon allume."`
       - Per VOICE-02: This proves the text response reaches TTS via IntentResponse.speech.

    5. **test_conversation_result_has_speech(hass, mock_config_entry):**
       - Same setup and mock as test_response_speech_set but with text `"eteins la cuisine"` and router returning `"Cuisine eteinte."`.
       - `assert isinstance(result.response, conversation_intent.IntentResponse)`
       - `assert "plain" in result.response.speech`
       - `assert len(result.response.speech["plain"]["speech"]) > 0`
       - Per VOICE-02: Pipeline reads `.response.speech` to forward text to TTS engine.

    **Important mock notes:**
    - Patch `custom_components.ha_ai_agent.intent_router.IntentRouter.async_route` at the module level.
    - The `mock_config_entry` fixture already sets up homeassistant core component and adds the entry to hass.
    - After `async_setup`, the entity is accessible via `hass.data["entity_components"]["conversation"]` or by using the conversation platform helpers.
    - Do NOT import or patch `ClaudeClient` — tests 3 and 4 use router return values that skip the LLM branch.

    **Note on ConversationInput constructor:** Existing tests in `test_conversation_bridge.py` use `ConversationInput(text=..., context=Context(), conversation_id=None, device_id=None, language="fr", agent_id=None)`. The decision from Phase 2 says "ConversationInput HA 2026.x requires device_id and satellite_id positional args — tests must include both as None". So use: `ConversationInput(text=..., context=Context(), conversation_id=None, device_id=None, language="fr", agent_id=None, satellite_id=None)`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_voice_pipeline.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_voice_pipeline.py` exists and contains `def test_agent_discoverable_by_pipeline(`
    - `tests/test_voice_pipeline.py` contains `def test_agent_entity_id_format(`
    - `tests/test_voice_pipeline.py` contains `def test_response_speech_set(`
    - `tests/test_voice_pipeline.py` contains `def test_conversation_result_has_speech(`
    - `tests/test_voice_pipeline.py` contains `async_get_agent_info(hass, "conversation.ha_ai_agent")`
    - `tests/test_voice_pipeline.py` contains `async_set_speech` (verifying speech output path)
    - `python -m pytest tests/test_voice_pipeline.py -x -q` exits with code 0 and reports 4 passed
    - `python -m pytest tests/ -x -q` exits with code 0 (full suite still green)
  </acceptance_criteria>
  <done>4 tests pass: agent discoverable by pipeline, entity_id has conversation. prefix, response speech set, conversation result has speech dict populated. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Configure and verify HA voice pipeline end-to-end</name>
  <files>N/A</files>
  <read_first>
    - .planning/phases/04-voice-pipeline/04-CONTEXT.md (D-01 through D-06: pipeline config values)
    - .planning/phases/04-voice-pipeline/04-RESEARCH.md (Pitfalls 1-5, Configuration UI Flow)
    - .planning/phases/04-voice-pipeline/04-UI-SPEC.md (Interaction Contract steps 1-5)
  </read_first>
  <action>
    Human-only task: configure the HA voice pipeline via the native UI. All configuration values are specified in D-05/D-06 from CONTEXT.md. Install Wyoming Whisper (STT) and Wyoming Piper (TTS) add-ons, create an Assist pipeline named "Mon assistant HA" selecting conversation.ha_ai_agent as conversation engine, STT=faster-whisper (fr), TTS=Piper with voice fr_FR-siwis-medium, wake word=openWakeWord/alexa (optional, non-commercial only per D-02). Then run WebSocket smoke test to validate end-to-end.
  </action>
  <what-built>
    Unit tests confirm the agent is pipeline-compatible. Now the HA voice pipeline must be configured in the live HA instance and verified end-to-end.
  </what-built>
  <how-to-verify>
    **Step 1 — Install add-ons (HA OS/Supervised only):**
    1. Go to Settings > Add-ons > Add-on Store
    2. Search "Whisper" > Install "Whisper" > Start > Enable "Start on boot"
    3. Search "Piper" > Install "Piper" > Start > Enable "Start on boot"
    4. (Optional) Search "openWakeWord" > Install > Start > Enable "Start on boot"

    **Step 2 — Verify Wyoming integrations discovered:**
    1. Go to Settings > Devices and Services
    2. Confirm "Wyoming Protocol" integration shows Whisper (port 10300) and Piper (port 10200)
    3. If not auto-discovered: Add Integration > Wyoming Protocol > host=localhost, port=10300 (Whisper) and repeat for port=10200 (Piper)
    4. Expected: "faster-whisper" appears in STT engine list, "Piper" appears in TTS engine list

    **Step 3 — Create Assist pipeline (per D-05, D-06):**
    1. Go to Settings > Voice Assistants > Add Assistant
    2. Name: "Mon assistant HA"
    3. Conversation agent: select "HA AI Agent" from dropdown (entity conversation.ha_ai_agent per D-06)
    4. Language: Francais (fr) per D-06
    5. Speech-to-text: select "faster-whisper", language "fr" per D-06
    6. Text-to-speech: select "Piper", language "fr", voice "fr_FR-siwis-medium" per D-04
    7. Wake word: select "openWakeWord", word "alexa" per D-02, D-03 (NOTE: alexa wake word model is non-commercial use only)
    8. Click Save

    **Step 4 — Smoke test (text-only via pipeline):**
    1. Open HA Developer Tools > WebSocket
    2. Send: `{"type": "assist_pipeline/pipeline/list"}`
    3. Verify response contains a pipeline where `"conversation_engine": "conversation.ha_ai_agent"` (NOT "ha_ai_agent" without prefix — Pitfall 1)
    4. Get the pipeline ID from the list response
    5. Send: `{"type": "assist_pipeline/run", "start_stage": "intent", "end_stage": "intent", "input": {"text": "allume le salon"}, "pipeline": "<pipeline_id>"}`
    6. Verify: event `intent-end` received with non-empty `speech` field containing the agent response

    **Step 5 — Voice end-to-end (if microphone available):**
    1. Say the wake word ("alexa") near the microphone
    2. Say "allume le salon"
    3. Verify: the correct entity changes state AND a spoken response is heard via TTS

    **Expected outcomes:**
    - Pipeline list shows conversation.ha_ai_agent as conversation_engine
    - WebSocket smoke test returns intent-end with speech text
    - (If mic available) Voice command controls entity and TTS speaks response
  </how-to-verify>
  <verify>Human verification — see how-to-verify steps above</verify>
  <acceptance_criteria>
    - WebSocket `assist_pipeline/pipeline/list` response contains `"conversation_engine": "conversation.ha_ai_agent"`
    - WebSocket `assist_pipeline/run` with `start_stage: "intent"` returns event with non-empty `speech` field
    - Pipeline named "Mon assistant HA" visible in Settings > Voice Assistants
  </acceptance_criteria>
  <done>HA voice pipeline "Mon assistant HA" configured and verified via WebSocket smoke test. Pipeline routes to conversation.ha_ai_agent and returns speech response.</done>
  <resume-signal>Type "approved" if pipeline is working end-to-end, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_voice_pipeline.py -x -q` — all 4 tests pass
2. `python -m pytest tests/ -x -q` — full suite green (no regressions)
3. HA pipeline list shows `conversation_engine: "conversation.ha_ai_agent"` (human verified)
4. WebSocket smoke test `assist_pipeline/run` returns speech response (human verified)
</verification>

<success_criteria>
- 4 unit tests in test_voice_pipeline.py all pass, proving VOICE-01 (agent discoverable) and VOICE-02 (speech output populated)
- Full test suite remains green
- HA voice pipeline "Mon assistant HA" configured with STT=faster-whisper, TTS=Piper/fr_FR-siwis-medium, conversation_engine=conversation.ha_ai_agent
- WebSocket smoke test confirms pipeline routes to our agent and returns speech
</success_criteria>

<output>
After completion, create `.planning/phases/04-voice-pipeline/04-01-SUMMARY.md`
</output>
